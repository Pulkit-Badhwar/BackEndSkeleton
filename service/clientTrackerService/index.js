const fetchClientTracker = require('./fetchClientTracker');
const updateClientTracker = require('./updateClientTracker');
const findClientTracker = require('./findClientTracker');
const createClientTracker = require('./createClientTracker');
const inActiveClientTracker = require('./inActiveClientTracker');

module.exports = {
  fetchClientTracker,
  updateClientTracker,
  findClientTracker,
  createClientTracker,
  inActiveClientTracker,
};